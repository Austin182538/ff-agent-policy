# Requirements for the data ingestion / analysis tooling (scripts/, analysis/).
#
# nflreadpy requires Python >= 3.10, while the main app venv (requirements.txt)
# is pinned to Python 3.9. Use a separate virtual environment for this file:
#
#   py -3.13 -m venv venv_data
#   venv_data\Scripts\pip install -r requirements-data.txt   (Windows)
#   venv_data/bin/pip install -r requirements-data.txt        (macOS/Linux)
#
# These scripts still write into the same SQLite database (nfl_analytics.db)
# via the SQLAlchemy models in app/models, so SQLAlchemy + the app's config
# dependencies are included here too.

# nflverse historical data (stats, schedules, ADP/ECR rankings, opportunity)
nflreadpy>=0.1.5
pyarrow>=14.0.0

# Vegas odds (The Odds API) + general HTTP
requests>=2.31.0

# Shared with the main app (models/config/db access)
sqlalchemy>=2.0.23
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Data wrangling / analysis
pandas>=2.1.3
numpy>=1.26.0

# Isotonic regression + kernel smoothing for the ECR-to-points calibration
# curve in analysis/player_ranking_v1.py
scikit-learn>=1.4.0
